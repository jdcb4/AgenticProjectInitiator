import { Body, Code, Heading } from "@/components/ui/typography";
import { Stack, Surface } from "@/components/ui/surface";

export function App() {
  return (
    <Surface
      as="main"
      className="min-h-dvh flex flex-col items-center justify-center px-6"
    >
      <Stack gap="default" align="center" className="text-center max-w-prose">
        <Heading level={1}>{{project_name}}</Heading>
        <Body className="text-text-secondary">
          Scaffolded from the client-only preset. Edit{" "}
          <Code>src/app/App.tsx</Code> to start.
        </Body>
      </Stack>
    </Surface>
  );
}
