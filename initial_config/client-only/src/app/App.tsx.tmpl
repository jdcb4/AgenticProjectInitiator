export function App() {
  return (
    <main className="min-h-dvh flex flex-col items-center justify-center px-6 text-center">
      <h1 className="text-3xl font-semibold tracking-tight">{{project_name}}</h1>
      <p className="mt-3 max-w-prose text-neutral-400">
        Scaffolded from the client-only preset. Edit{" "}
        <code className="font-mono text-neutral-200">src/app/App.tsx</code> to start.
      </p>
    </main>
  );
}
