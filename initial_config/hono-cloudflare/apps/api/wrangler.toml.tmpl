name = "{{project_slug}}-api"
main = "src/index.ts"
compatibility_date = "{{year}}-01-01"
compatibility_flags = ["nodejs_compat"]

# Add KV / D1 / R2 / Durable Object bindings here as the project needs them.
# Document each binding in docs/ARCHITECTURE.md and docs/DECISIONS.md.

# [[kv_namespaces]]
# binding = "KV"
# id = "<replace-with-namespace-id>"

[vars]
LOG_LEVEL = "info"
